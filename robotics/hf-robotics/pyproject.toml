[project]
name = "hf-robotics"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Stefan Mesken", email = "kappadistributive@gmail.com" }
]
requires-python = ">=3.12.7"
dependencies = [
    "lerobot>=0.6.1",
]

[project.scripts]
hf-robotics = "hf_robotics:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
